openapi: 3.2.0
info:
  title: Appfire Worklogs API
  version: '1.0'
  description: 'Operations tagged Worklogs across 2 of this provider''s published API definitions: appfire-7pace-timetracker-v1-openapi-original.yml, appfire-7pace-timetracker-v2-openapi-original.yml. Each path carries the servers of the definition it was published in.'
servers:
- url: /
  description: Production API
security:
- bearer: []
tags:
- name: Worklogs
paths:
  /api/v1/worklogs:
    servers:
    - url: /
      description: Production API
    get:
      summary: Get a list of worklogs
      description: Returns a paginated list of worklogs with filters.
      parameters:
      - name: before
        in: query
        schema:
          type: string
        description: Cursor for pagination. Returns results before the specified cursor.
      - name: after
        in: query
        schema:
          type: string
        description: Cursor for pagination. Returns results after the specified cursor.
      - name: externalItemId
        in: query
        schema:
          type: array
          items:
            type: string
        description: Accepts a single string or array.
      - name: assigneeId
        in: query
        schema:
          type: array
          items:
            type: string
        description: Accepts a single string or array.
      - name: authorId
        in: query
        schema:
          type: array
          items:
            type: string
        description: Accepts a single string or array.
      - name: startedAt.start
        in: query
        schema:
          type: string
          format: date-time
        description: Filters worklogs that logged time starting after the specified date.
      - name: startedAt.end
        in: query
        schema:
          type: string
          format: date-time
        description: Filters worklogs that logged time starting before the specified date.
      - name: editedAt.start
        in: query
        schema:
          type: string
          format: date-time
        description: Filters worklogs that were updated after the specified date.
      - name: editedAt.end
        in: query
        schema:
          type: string
          format: date-time
        description: Filters worklogs that were updated before the specified date.
      - name: createdAt.start
        in: query
        schema:
          type: string
          format: date-time
        description: Filters worklogs that were created after the specified date.
      - name: createdAt.end
        in: query
        schema:
          type: string
          format: date-time
        description: Filters worklogs that were created before the specified date.
      responses:
        '200':
          description: A list of worklogs.
          content:
            application/json:
              schema:
                type: object
                properties:
                  result:
                    type: array
                    items:
                      $ref: '#/components/schemas/Worklog'
                  _links:
                    type: object
                    properties:
                      self:
                        type: object
                        properties:
                          href:
                            type: string
                      prev:
                        type: object
                        properties:
                          href:
                            type: string
                      next:
                        type: object
                        properties:
                          href:
                            type: string
                  pageInfo:
                    $ref: '#/components/schemas/PageInfo'
        '400':
          description: Error response for invalid filters.
          content:
            application/json:
              schema:
                type: object
                properties:
                  type:
                    type: string
                  title:
                    type: string
                  detail:
                    type: string
                  traceId:
                    type: string
      tags:
      - Worklogs
    post:
      summary: Create a worklog
      description: Creates a worklog.
      requestBody:
        description: Worklog object to create
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/WorklogRequest'
      responses:
        '200':
          description: Worklog created successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WorklogResponse'
        '400':
          description: Worklog cannot be created, invalid request body.
          content:
            application/json:
              schema:
                type: object
                properties:
                  type:
                    type: string
                  title:
                    type: string
                  detail:
                    type: string
                  traceId:
                    type: string
        '401':
          description: Unauthorized access. This indicates a problem with Authorization. Ensure apitoken is valid, and you are setting Authorization header to Bearer {apitoken}
        '403':
          description: Forbidden. The user does not have permission to create a worklog for the provided assigneeId.
          content:
            application/json:
              schema:
                type: object
                properties:
                  type:
                    type: string
                  title:
                    type: string
                  detail:
                    type: string
                  traceId:
                    type: string
        '404':
          description: The referenced worklog or assignee was not found.
          content:
            application/json:
              schema:
                type: object
                properties:
                  type:
                    type: string
                  title:
                    type: string
                  detail:
                    type: string
                  traceId:
                    type: string
        '500':
          description: Internal server error.
      tags:
      - Worklogs
  /api/v1/worklogs/{worklogId}:
    servers:
    - url: /
      description: Production API
    put:
      summary: Update a worklog
      description: Updates a worklog by its Id.
      parameters:
      - name: worklogId
        in: path
        required: true
        schema:
          type: string
        description: The Id of the worklog to update.
      requestBody:
        description: Worklog object to update
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/WorklogRequest'
      responses:
        '200':
          description: Worklog updated successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WorklogResponse'
        '400':
          description: Worklog cannot be updated - passed worklog ID or request body is invalid.
          content:
            application/json:
              schema:
                type: object
                properties:
                  type:
                    type: string
                  title:
                    type: string
                  detail:
                    type: string
                  traceId:
                    type: string
        '401':
          description: Unauthorized access. This indicates a problem with Authorization. Ensure apitoken is valid, and you are setting Authorization header to Bearer {apitoken}
        '403':
          description: Forbidden. The user associated with the apitoken does not have permission to update this worklog.
          content:
            application/json:
              schema:
                type: object
                properties:
                  type:
                    type: string
                  title:
                    type: string
                  detail:
                    type: string
                  traceId:
                    type: string
        '404':
          description: Worklog not found.
          content:
            application/json:
              schema:
                type: object
                properties:
                  type:
                    type: string
                  title:
                    type: string
                  detail:
                    type: string
                  traceId:
                    type: string
        '500':
          description: Internal server error.
      tags:
      - Worklogs
    delete:
      summary: Delete a worklog
      description: Deletes a worklog by its ID.
      parameters:
      - name: worklogId
        in: path
        required: true
        schema:
          type: string
        description: The ID of the worklog to delete.
      responses:
        '204':
          description: Worklog deleted successfully.
        '401':
          description: Unauthorized access. This indicates a problem with Authorization. Ensure apitoken is valid, and you are setting Authorization header to Bearer {apitoken}
        '403':
          description: Forbidden. The submitting user does not have permission to delete this worklog.
        '404':
          description: Worklog not found.
      tags:
      - Worklogs
  /api/v1/worklogs/changes:
    servers:
    - url: /
      description: Production API
    get:
      summary: Get a list of all worklogs, including deleted worklogs.
      description: To get a change list, use parameters editedAt.start and editedAt.end to return a list of all changes during the specified timeframe. All other filters can be used in combination. Use returned worklog field isDeleted to identify deleted worklogs.
      parameters:
      - name: before
        in: query
        schema:
          type: string
        description: Cursor for pagination. Returns results before the specified cursor.
      - name: after
        in: query
        schema:
          type: string
        description: Cursor for pagination. Returns results after the specified cursor.
      - name: externalItemId
        in: query
        schema:
          type: array
          items:
            type: string
        description: Accepts a single string or array.
      - name: assigneeId
        in: query
        schema:
          type: array
          items:
            type: string
        description: Accepts a single string or array.
      - name: authorId
        in: query
        schema:
          type: array
          items:
            type: string
        description: Accepts a single string or array.
      - name: startedAt.start
        in: query
        schema:
          type: string
          format: date-time
        description: Filters worklogs that logged time starting after the specified date.
      - name: startedAt.end
        in: query
        schema:
          type: string
          format: date-time
        description: Filters worklogs that logged time starting before the specified date.
      - name: editedAt.start
        in: query
        schema:
          type: string
          format: date-time
        description: Filters worklogs that were updated after the specified date.
      - name: editedAt.end
        in: query
        schema:
          type: string
          format: date-time
        description: Filters worklogs that were updated before the specified date.
      - name: createdAt.start
        in: query
        schema:
          type: string
          format: date-time
        description: Filters worklogs that were created after the specified date.
      - name: createdAt.end
        in: query
        schema:
          type: string
          format: date-time
        description: Filters worklogs that were created before the specified date.
      responses:
        '200':
          description: A list of worklogs.
          content:
            application/json:
              schema:
                type: object
                properties:
                  result:
                    type: array
                    items:
                      $ref: '#/components/schemas/ChangedWorklog'
                  _links:
                    type: object
                    properties:
                      self:
                        type: object
                        properties:
                          href:
                            type: string
                      prev:
                        type: object
                        properties:
                          href:
                            type: string
                      next:
                        type: object
                        properties:
                          href:
                            type: string
                  pageInfo:
                    $ref: '#/components/schemas/PageInfo'
        '400':
          description: Error response for invalid filters.
          content:
            application/json:
              schema:
                type: object
                properties:
                  type:
                    type: string
                  title:
                    type: string
                  detail:
                    type: string
                  traceId:
                    type: string
      tags:
      - Worklogs
  /api/v1/worklogs/migrated:
    servers:
    - url: /
      description: Production API
    post:
      summary: Create a migrated worklog
      description: Creates a migrated worklog. Requires ADMIN permission (Manage 7pace settings and assign roles to users). Migrated worklogs bypass tracking-details, locked-date, and closed-item validations, and are not synchronized to Jira.
      requestBody:
        description: Worklog object to create as migrated
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/WorklogRequest'
      responses:
        '200':
          description: Migrated worklog created successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WorklogResponse'
        '400':
          description: Worklog cannot be created, invalid request body.
          content:
            application/json:
              schema:
                type: object
                properties:
                  type:
                    type: string
                  title:
                    type: string
                  detail:
                    type: string
                  traceId:
                    type: string
        '401':
          description: Unauthorized access. This indicates a problem with Authorization. Ensure apitoken is valid, and you are setting Authorization header to Bearer {apitoken}
        '403':
          description: Forbidden. The user does not have permission to create migrated worklogs.
          content:
            application/json:
              schema:
                type: object
                properties:
                  type:
                    type: string
                  title:
                    type: string
                  detail:
                    type: string
                  traceId:
                    type: string
        '404':
          description: The referenced worklog or assignee was not found.
          content:
            application/json:
              schema:
                type: object
                properties:
                  type:
                    type: string
                  title:
                    type: string
                  detail:
                    type: string
                  traceId:
                    type: string
        '500':
          description: Internal server error.
      tags:
      - Worklogs
  /api/v1/worklogs/migrated/{worklogId}:
    servers:
    - url: /
      description: Production API
    delete:
      summary: Delete a migrated worklog
      description: Deletes a migrated worklog by its ID. Requires ADMIN permission (Manage 7pace settings and assign roles to users).
      parameters:
      - name: worklogId
        in: path
        required: true
        schema:
          type: string
        description: The ID of the migrated worklog to delete.
      responses:
        '204':
          description: Migrated worklog deleted successfully.
        '400':
          description: The worklog is not migrated.
          content:
            application/json:
              schema:
                type: object
                properties:
                  type:
                    type: string
                  title:
                    type: string
                  detail:
                    type: string
                  traceId:
                    type: string
        '401':
          description: Unauthorized access. This indicates a problem with Authorization. Ensure apitoken is valid, and you are setting Authorization header to Bearer {apitoken}
        '403':
          description: Forbidden. The user does not have permission to delete migrated worklogs.
          content:
            application/json:
              schema:
                type: object
                properties:
                  type:
                    type: string
                  title:
                    type: string
                  detail:
                    type: string
                  traceId:
                    type: string
        '404':
          description: Worklog not found.
          content:
            application/json:
              schema:
                type: object
                properties:
                  type:
                    type: string
                  title:
                    type: string
                  detail:
                    type: string
                  traceId:
                    type: string
        '500':
          description: Internal server error.
      tags:
      - Worklogs
  /api/v2/worklogs:
    servers:
    - url: /
      description: Production API
    get:
      summary: Get a list of worklogs
      description: Returns a paginated list of worklogs with filters.
      parameters:
      - name: before
        in: query
        schema:
          type: string
        description: Cursor for pagination. Returns results before the specified cursor.
      - name: after
        in: query
        schema:
          type: string
        description: Cursor for pagination. Returns results after the specified cursor.
      - name: externalItemId
        in: query
        schema:
          type: array
          items:
            type: string
        description: Accepts a single string or array.
      - name: assigneeId
        in: query
        schema:
          type: array
          items:
            type: string
        description: Accepts a single string or array.
      - name: authorId
        in: query
        schema:
          type: array
          items:
            type: string
        description: Accepts a single string or array.
      - name: startedAt.start
        in: query
        schema:
          type: string
          format: date-time
        description: Filters worklogs that logged time starting after the specified date.
      - name: startedAt.end
        in: query
        schema:
          type: string
          format: date-time
        description: Filters worklogs that logged time starting before the specified date.
      - name: editedAt.start
        in: query
        schema:
          type: string
          format: date-time
        description: Filters worklogs that were updated after the specified date.
      - name: editedAt.end
        in: query
        schema:
          type: string
          format: date-time
        description: Filters worklogs that were updated before the specified date.
      - name: createdAt.start
        in: query
        schema:
          type: string
          format: date-time
        description: Filters worklogs that were created after the specified date.
      - name: createdAt.end
        in: query
        schema:
          type: string
          format: date-time
        description: Filters worklogs that were created before the specified date.
      responses:
        '200':
          description: A list of worklogs.
          content:
            application/json:
              schema:
                type: object
                properties:
                  result:
                    type: array
                    items:
                      $ref: '#/components/schemas/Worklog_2'
                  _links:
                    type: object
                    properties:
                      self:
                        type: object
                        properties:
                          href:
                            type: string
                      prev:
                        type: object
                        properties:
                          href:
                            type: string
                      next:
                        type: object
                        properties:
                          href:
                            type: string
                  pageInfo:
                    $ref: '#/components/schemas/PageInfo'
        '400':
          description: Error response for invalid filters.
          content:
            application/json:
              schema:
                type: object
                properties:
                  type:
                    type: string
                  title:
                    type: string
                  detail:
                    type: string
                  traceId:
                    type: string
      tags:
      - Worklogs
    post:
      summary: Create a worklog
      description: Creates a worklog.
      requestBody:
        description: Worklog object to create
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/WorklogRequest_2'
      responses:
        '200':
          description: Worklog created successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WorklogResponse'
        '400':
          description: Worklog cannot be created, invalid request body.
          content:
            application/json:
              schema:
                type: object
                properties:
                  type:
                    type: string
                  title:
                    type: string
                  detail:
                    type: string
                  traceId:
                    type: string
        '401':
          description: Unauthorized access. This indicates a problem with Authorization. Ensure apitoken is valid, and you are setting Authorization header to Bearer {apitoken}
        '403':
          description: Forbidden. The user does not have permission to create a worklog for the provided assigneeId.
          content:
            application/json:
              schema:
                type: object
                properties:
                  type:
                    type: string
                  title:
                    type: string
                  detail:
                    type: string
                  traceId:
                    type: string
        '404':
          description: The referenced worklog or assignee was not found.
          content:
            application/json:
              schema:
                type: object
                properties:
                  type:
                    type: string
                  title:
                    type: string
                  detail:
                    type: string
                  traceId:
                    type: string
        '500':
          description: Internal server error.
      tags:
      - Worklogs
  /api/v2/worklogs/{worklogId}:
    servers:
    - url: /
      description: Production API
    put:
      summary: Update a worklog
      description: Updates a worklog by its Id.
      parameters:
      - name: worklogId
        in: path
        required: true
        schema:
          type: string
        description: The Id of the worklog to update.
      requestBody:
        description: Worklog object to update
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/WorklogRequest_2'
      responses:
        '200':
          description: Worklog updated successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WorklogResponse'
        '400':
          description: Worklog cannot be updated - passed worklog ID or request body is invalid.
          content:
            application/json:
              schema:
                type: object
                properties:
                  type:
                    type: string
                  title:
                    type: string
                  detail:
                    type: string
                  traceId:
                    type: string
        '401':
          description: Unauthorized access. This indicates a problem with Authorization. Ensure apitoken is valid, and you are setting Authorization header to Bearer {apitoken}
        '403':
          description: Forbidden. The user associated with the apitoken does not have permission to update this worklog.
          content:
            application/json:
              schema:
                type: object
                properties:
                  type:
                    type: string
                  title:
                    type: string
                  detail:
                    type: string
                  traceId:
                    type: string
        '404':
          description: Worklog not found.
          content:
            application/json:
              schema:
                type: object
                properties:
                  type:
                    type: string
                  title:
                    type: string
                  detail:
                    type: string
                  traceId:
                    type: string
        '500':
          description: Internal server error.
      tags:
      - Worklogs
    delete:
      summary: Delete a worklog
      description: Deletes a worklog by its ID.
      parameters:
      - name: worklogId
        in: path
        required: true
        schema:
          type: string
        description: The ID of the worklog to delete.
      responses:
        '204':
          description: Worklog deleted successfully.
        '401':
          description: Unauthorized access. This indicates a problem with Authorization. Ensure apitoken is valid, and you are setting Authorization header to Bearer {apitoken}
        '403':
          description: Forbidden. The submitting user does not have permission to delete this worklog.
        '404':
          description: Worklog not found.
      tags:
      - Worklogs
  /api/v2/worklogs/views/incrementalChanges:
    servers:
    - url: /
      description: Production API
    get:
      summary: Get a list of all worklogs, including deleted worklogs.
      description: To get a change list, use parameters editedAt.start and editedAt.end to return a list of all changes during the specified timeframe. All other filters can be used in combination. Use returned worklog field isDeleted to identify deleted worklogs.
      parameters:
      - name: before
        in: query
        schema:
          type: string
        description: Cursor for pagination. Returns results before the specified cursor.
      - name: after
        in: query
        schema:
          type: string
        description: Cursor for pagination. Returns results after the specified cursor.
      - name: externalItemId
        in: query
        schema:
          type: array
          items:
            type: string
        description: Accepts a single string or array.
      - name: assigneeId
        in: query
        schema:
          type: array
          items:
            type: string
        description: Accepts a single string or array.
      - name: authorId
        in: query
        schema:
          type: array
          items:
            type: string
        description: Accepts a single string or array.
      - name: startedAt.start
        in: query
        schema:
          type: string
          format: date-time
        description: Filters worklogs that logged time starting after the specified date.
      - name: startedAt.end
        in: query
        schema:
          type: string
          format: date-time
        description: Filters worklogs that logged time starting before the specified date.
      - name: editedAt.start
        in: query
        schema:
          type: string
          format: date-time
        description: Filters worklogs that were updated after the specified date.
      - name: editedAt.end
        in: query
        schema:
          type: string
          format: date-time
        description: Filters worklogs that were updated before the specified date.
      - name: createdAt.start
        in: query
        schema:
          type: string
          format: date-time
        description: Filters worklogs that were created after the specified date.
      - name: createdAt.end
        in: query
        schema:
          type: string
          format: date-time
        description: Filters worklogs that were created before the specified date.
      responses:
        '200':
          description: A list of worklogs.
          content:
            application/json:
              schema:
                type: object
                properties:
                  result:
                    type: array
                    items:
                      $ref: '#/components/schemas/ChangedWorklog_2'
                  _links:
                    type: object
                    properties:
                      self:
                        type: object
                        properties:
                          href:
                            type: string
                      prev:
                        type: object
                        properties:
                          href:
                            type: string
                      next:
                        type: object
                        properties:
                          href:
                            type: string
                  pageInfo:
                    $ref: '#/components/schemas/PageInfo'
        '400':
          description: Error response for invalid filters.
          content:
            application/json:
              schema:
                type: object
                properties:
                  type:
                    type: string
                  title:
                    type: string
                  detail:
                    type: string
                  traceId:
                    type: string
      tags:
      - Worklogs
  /api/v2/worklogs/migrated:
    servers:
    - url: /
      description: Production API
    post:
      summary: Create a migrated worklog
      description: Creates a migrated worklog. Requires ADMIN permission (Manage 7pace settings and assign roles to users). Migrated worklogs bypass tracking-details, locked-date, and closed-item validations, and are not synchronized to Jira.
      requestBody:
        description: Worklog object to create as migrated
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/WorklogRequest_2'
      responses:
        '200':
          description: Migrated worklog created successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WorklogResponse'
        '400':
          description: Worklog cannot be created, invalid request body.
          content:
            application/json:
              schema:
                type: object
                properties:
                  type:
                    type: string
                  title:
                    type: string
                  detail:
                    type: string
                  traceId:
                    type: string
        '401':
          description: Unauthorized access. This indicates a problem with Authorization. Ensure apitoken is valid, and you are setting Authorization header to Bearer {apitoken}
        '403':
          description: Forbidden. The user does not have permission to create migrated worklogs.
          content:
            application/json:
              schema:
                type: object
                properties:
                  type:
                    type: string
                  title:
                    type: string
                  detail:
                    type: string
                  traceId:
                    type: string
        '404':
          description: The referenced worklog or assignee was not found.
          content:
            application/json:
              schema:
                type: object
                properties:
                  type:
                    type: string
                  title:
                    type: string
                  detail:
                    type: string
                  traceId:
                    type: string
        '500':
          description: Internal server error.
      tags:
      - Worklogs
  /api/v2/worklogs/migrated/{worklogId}:
    servers:
    - url: /
      description: Production API
    delete:
      summary: Delete a migrated worklog
      description: Deletes a migrated worklog by its ID. Requires ADMIN permission (Manage 7pace settings and assign roles to users).
      parameters:
      - name: worklogId
        in: path
        required: true
        schema:
          type: string
        description: The ID of the migrated worklog to delete.
      responses:
        '204':
          description: Migrated worklog deleted successfully.
        '400':
          description: The worklog is not migrated.
          content:
            application/json:
              schema:
                type: object
                properties:
                  type:
                    type: string
                  title:
                    type: string
                  detail:
                    type: string
                  traceId:
                    type: string
        '401':
          description: Unauthorized access. This indicates a problem with Authorization. Ensure apitoken is valid, and you are setting Authorization header to Bearer {apitoken}
        '403':
          description: Forbidden. The user does not have permission to delete migrated worklogs.
          content:
            application/json:
              schema:
                type: object
                properties:
                  type:
                    type: string
                  title:
                    type: string
                  detail:
                    type: string
                  traceId:
                    type: string
        '404':
          description: Worklog not found.
          content:
            application/json:
              schema:
                type: object
                properties:
                  type:
                    type: string
                  title:
                    type: string
                  detail:
                    type: string
                  traceId:
                    type: string
        '500':
          description: Internal server error.
      tags:
      - Worklogs
components:
  schemas:
    CustomFieldRequest:
      type: object
      description: A custom field representing a boolean toggle.
      required:
      - id
      - type
      properties:
        id:
          type: string
        type:
          type: string
    CustomFields:
      type: object
      description: Custom fields represents user-defined fields for additional worklog data, such as toggles and dropdowns configured in application settings.
      properties:
        toggle:
          type: array
          items:
            $ref: '#/components/schemas/ToggleField'
        dropdown:
          type: array
          items:
            $ref: '#/components/schemas/DropdownField'
    ChangedWorklog_2:
      type: object
      properties:
        id:
          type: string
          format: uuid
        isDeleted:
          type: boolean
          description: Indicates if the worklog has been deleted.
        isMigrated:
          type: boolean
          description: Indicates if the worklog is migrated.
        comment:
          type: string
        startedAt:
          type: string
          format: date-time
          example: '2025-07-15T09:30:11'
        createdAt:
          type: string
          format: date-time
        editedAt:
          type: string
          format: date-time
        assigneeId:
          type: string
        authorId:
          type: string
        accountId:
          type: string
        editorId:
          type: string
        duration:
          type: integer
        externalItemId:
          type: string
        customFields:
          $ref: '#/components/schemas/CustomFieldsResponse'
    CustomFieldsResponse:
      type: array
      description: A list of user-defined fields for additional worklog data. Each field has a 'type' property that determines its structure and value.
      items:
        oneOf:
        - $ref: '#/components/schemas/ToggleFieldResponse'
        - $ref: '#/components/schemas/DropdownFieldResponse'
        discriminator:
          propertyName: type
          mapping:
            toggle: '#/components/schemas/ToggleFieldResponse'
            dropdown: '#/components/schemas/DropdownFieldResponse'
    ToggleFieldRequest:
      allOf:
      - $ref: '#/components/schemas/CustomFieldRequest'
      - type: object
        properties:
          value:
            type: boolean
    ToggleField:
      type: object
      properties:
        id:
          type: string
          description: Identifier of the toggle field.
        value:
          type: boolean
    ToggleFieldResponse:
      allOf:
      - $ref: '#/components/schemas/CustomFieldResponse'
      - type: object
        properties:
          value:
            type: boolean
            description: The boolean value of the toggle.
    WorklogRequest_2:
      type: object
      required:
      - startedAt
      - duration
      properties:
        comment:
          type: string
          description: Description or notes about the worklog.
        startedAt:
          type: string
          format: date-time
          description: Date and time of starting work in the format YYYY-MM-DDTHH:mm:ss or YYYY-MM-DDTHH:mm:ss±hh:mm. If a timezone offset is provided, it will be ignored and the time will be treated as local time.
          example: '2025-07-01T12:00:00'
        assigneeId:
          type: string
          description: Identifier of the user assigned to the worklog. If not provided, the user creating the worklog will be assigned by default.
          example: 5b20f3469bcd7518af217f81
        duration:
          type: integer
          description: Duration of the work in seconds.
          example: 3600
        externalItemId:
          type: string
          description: The item id the worklog is associated with (jira work item, for example)
          example: '423432'
        customFields:
          $ref: '#/components/schemas/CustomFieldsRequest'
    WorklogResponse:
      type: object
      properties:
        worklog:
          $ref: '#/components/schemas/Worklog'
    DropdownValueRequest:
      type: object
      properties:
        id:
          type: string
    Worklog:
      type: object
      properties:
        id:
          type: string
          format: uuid
        comment:
          type: string
        startedAt:
          type: string
          format: date-time
          example: '2025-07-15T09:30:11'
        createdAt:
          type: string
          format: date-time
        editedAt:
          type: string
          format: date-time
        assigneeId:
          type: string
        authorId:
          type: string
        accountId:
          type: string
        editorId:
          type: string
        duration:
          type: integer
        isMigrated:
          type: boolean
          description: Indicates if the worklog is migrated.
        externalItemId:
          type: string
        customFields:
          $ref: '#/components/schemas/CustomFields'
    DropdownValueResponse:
      type: object
      description: Represents the selected option in a dropdown field.
      properties:
        id:
          type: string
          description: Identifier of the selected option.
        name:
          type: string
          description: Name of the selected option.
    Worklog_2:
      type: object
      properties:
        id:
          type: string
          format: uuid
        comment:
          type: string
        startedAt:
          type: string
          format: date-time
          example: '2025-07-15T09:30:11'
        createdAt:
          type: string
          format: date-time
        editedAt:
          type: string
          format: date-time
        assigneeId:
          type: string
        authorId:
          type: string
        accountId:
          type: string
        editorId:
          type: string
        duration:
          type: integer
        isMigrated:
          type: boolean
          description: Indicates if the worklog is migrated.
        externalItemId:
          type: string
        customFields:
          $ref: '#/components/schemas/CustomFieldsResponse'
    CustomFieldsRequest:
      type: array
      description: A list of user-defined fields for additional worklog data. Each field has a 'type' property that determines its structure and value.
      items:
        oneOf:
        - $ref: '#/components/schemas/ToggleFieldRequest'
        - $ref: '#/components/schemas/DropdownFieldRequest'
        discriminator:
          propertyName: type
          mapping:
            toggle: '#/components/schemas/ToggleFieldRequest'
            dropdown: '#/components/schemas/DropdownFieldRequest'
    ChangedWorklog:
      type: object
      properties:
        id:
          type: string
          format: uuid
        isDeleted:
          type: boolean
          description: Indicates if the worklog has been deleted.
        isMigrated:
          type: boolean
          description: Indicates if the worklog is migrated.
        comment:
          type: string
        startedAt:
          type: string
          format: date-time
          example: '2025-07-15T09:30:11'
        createdAt:
          type: string
          format: date-time
        editedAt:
          type: string
          format: date-time
        assigneeId:
          type: string
        authorId:
          type: string
        accountId:
          type: string
        editorId:
          type: string
        duration:
          type: integer
        externalItemId:
          type: string
        customFields:
          $ref: '#/components/schemas/CustomFields'
    DropdownField:
      type: object
      properties:
        id:
          type: string
          description: Identifier of the dropdown field.
        value:
          type: array
          items:
            type: string
            description: Selected option identifier for the dropdown field.
    CustomFieldResponse:
      type: object
      required:
      - id
      - type
      - name
      properties:
        id:
          type: string
          description: Identifier of the custom field.
        type:
          type: string
          description: The type of the custom field, which determines the structure of the 'value' property.
        name:
          type: string
          description: Name of the custom field.
    DropdownFieldResponse:
      type: object
      description: A custom field representing a single-select dropdown.
      allOf:
      - $ref: '#/components/schemas/CustomFieldResponse'
      - type: object
        properties:
          value:
            $ref: '#/components/schemas/DropdownValueResponse'
    DropdownFieldRequest:
      allOf:
      - $ref: '#/components/schemas/CustomFieldRequest'
      - type: object
        properties:
          value:
            $ref: '#/components/schemas/DropdownValueRequest'
    PageInfo:
      type: object
      properties:
        hasNextPage:
          type: boolean
        hasPreviousPage:
          type: boolean
        startCursor:
          type: string
        endCursor:
          type: string
    WorklogRequest:
      type: object
      required:
      - startedAt
      - duration
      properties:
        comment:
          type: string
          description: Description or notes about the worklog.
        startedAt:
          type: string
          format: date-time
          description: Date and time of starting work in the format YYYY-MM-DDTHH:mm:ss or YYYY-MM-DDTHH:mm:ss±hh:mm. If a timezone offset is provided, it will be ignored and the time will be treated as local time.
          example: '2025-07-01T12:00:00'
        assigneeId:
          type: string
          description: Identifier of the user assigned to the worklog. If not provided, the user creating the worklog will be assigned by default.
          example: 5b20f3469bcd7518af217f81
        duration:
          type: integer
          description: Duration of the work in seconds.
          example: 3600
        externalItemId:
          type: string
          description: The item id the worklog is associated with (jira work item, for example)
          example: '423432'
        customFields:
          $ref: '#/components/schemas/CustomFields'
  securitySchemes:
    bearer:
      type: http
      scheme: bearer
x-refined-from:
- appfire-7pace-timetracker-v1-openapi-original.yml
- appfire-7pace-timetracker-v2-openapi-original.yml
